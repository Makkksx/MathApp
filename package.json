{
  "name": "deploy-heroku",
  "version": "1.0.0",
  "description": "back-end for the front-end using Heroku",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ./frontend && npm run build --prod && cp -r build ../"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1",
    "pg": "^8.5.1"
  }
}
